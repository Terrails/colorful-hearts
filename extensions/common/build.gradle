architectury {
    common("fabric", "neoforge")
}

dependencies {
    compileOnly "net.fabricmc:fabric-loader:${project.fabric_loader_version}"

    compileOnly(project(path: ':api:common')) { transitive false }
}

modLoader = "javafml"
loaderVersion = "[2,)"
issueTrackerURL = "https://github.com/Terrails/colorful-hearts/issues"
license = "MIT"

[[mods]]
modId = "colorfulhearts"
version = "${mod_version}"

displayName = "Colorful Hearts"
authors = "Terrails"
description = ''' A client side mod that replaces multiple vanilla heart rows with a single row using colored hearts '''

logoFile = "icon.png"
displayTest = "IGNORE_ALL_VERSION" # ignore everything since it's a client only mod

[[mixins]]
config = "colorfulhearts-common.mixins.json"

[[dependencies.colorfulhearts]]
modId = "minecraft"
type = "required"
versionRange = "[${min_minecraft_version},)"
ordering = "NONE"
side = "CLIENT"

[[dependencies.colorfulhearts]]
modId = "neoforge"
type = "required"
versionRange = "[${min_neoforge_version},)"
ordering = "NONE"
side = "CLIENT"
